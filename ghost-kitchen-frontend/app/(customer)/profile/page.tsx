import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function ProfilePage() {
  return (
    <PagePlaceholder
      description="Profile details, addresses, saved cards, notifications, and logout settings belong on this page."
      eyebrow="Customer"
      title="Profile"
    />
  );
}
