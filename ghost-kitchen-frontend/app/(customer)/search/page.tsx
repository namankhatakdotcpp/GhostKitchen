import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function SearchPage() {
  return (
    <PagePlaceholder
      description="Dedicated search results, cuisine suggestions, and dish-first ranking live here. The home feed search is already wired and ready to connect to backend APIs."
      eyebrow="Customer"
      title="Search"
    />
  );
}
