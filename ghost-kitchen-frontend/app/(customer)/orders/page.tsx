import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function OrdersPage() {
  return (
    <PagePlaceholder
      description="Order history, reorder actions, invoices, and status snapshots surface here."
      eyebrow="Customer"
      title="Past Orders"
    />
  );
}
