import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function CartPage() {
  return (
    <PagePlaceholder
      description="Cart summary, item editing, coupon application, and checkout CTA plug into the migrated Zustand cart store here."
      eyebrow="Customer"
      title="Cart"
    />
  );
}
