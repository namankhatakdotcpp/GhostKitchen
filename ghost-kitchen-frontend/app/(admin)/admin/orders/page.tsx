import { PagePlaceholder } from "@/components/ui/page-placeholder";

export default function AdminOrdersPage() {
  return (
    <PagePlaceholder
      description="Dedicated order queue management can split out of the dashboard once operations volume justifies a separate triage surface."
      eyebrow="Admin"
      title="Orders"
    />
  );
}
